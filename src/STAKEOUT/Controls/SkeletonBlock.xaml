<UserControl x:Class="Stakeout.Controls.SkeletonBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Stakeout.Controls"
             IsHitTestVisible="False" Focusable="False">
    <!-- Placeholder bar: dark base + running highlight. Size it like the text it
         stands in for (Width/Height set by the consumer). -->
    <Grid>
        <Border Background="#FF171717" />
        <c:ShimmerOverlay />
    </Grid>
</UserControl>
